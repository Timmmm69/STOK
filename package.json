{
  "name": "stok",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": "24.14.0",
    "pnpm": "11.9.0"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint . --max-warnings=0",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config vitest.config.ts",
    "test:integration": "cross-env DATABASE_URL=postgresql://stok:stok_test@localhost:55433/stok_test?schema=public prisma migrate reset --force && cross-env DATABASE_URL=postgresql://stok:stok_test@localhost:55433/stok_test?schema=public AUTH_ALLOWED_EMAIL=owner@example.com prisma db seed && cross-env DATABASE_URL=postgresql://stok:stok_test@localhost:55433/stok_test?schema=public AUTH_ALLOWED_EMAIL=owner@example.com vitest run --config vitest.integration.config.ts",
    "test:e2e": "playwright test",
    "work:check": "tsx scripts/workboard.ts check",
    "work:render": "tsx scripts/workboard.ts render",
    "work:github:plan": "tsx scripts/github-board.ts plan",
    "work:github:apply": "tsx scripts/github-board.ts apply",
    "db:up": "docker compose up -d --wait postgres postgres-test",
    "db:down": "docker compose down",
    "db:migrate": "prisma migrate deploy",
    "db:seed": "prisma db seed",
    "db:reset": "prisma migrate reset --force && prisma db seed",
    "check:architecture": "dependency-cruiser src --config dependency-cruiser.config.cjs && tsx scripts/quality.ts && pnpm work:check && prisma validate",
    "check": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm check:architecture && pnpm test",
    "check:full": "pnpm check && pnpm db:up && pnpm test:integration && pnpm build && cross-env DATABASE_URL=postgresql://stok:stok_test@localhost:55433/stok_test?schema=public pnpm test:e2e",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@better-auth/prisma-adapter": "1.6.23",
    "@prisma/adapter-pg": "7.8.0",
    "@prisma/client": "7.8.0",
    "better-auth": "1.6.23",
    "dotenv": "17.4.2",
    "next": "16.2.10",
    "pg": "8.22.0",
    "pino": "10.3.1",
    "react": "19.2.7",
    "react-dom": "19.2.7",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "@types/node": "26.1.1",
    "@types/pg": "8.20.0",
    "@types/react": "19.2.17",
    "@types/react-dom": "19.2.3",
    "cross-env": "10.1.0",
    "dependency-cruiser": "18.1.0",
    "eslint": "9.39.5",
    "eslint-config-next": "16.2.10",
    "prettier": "3.9.5",
    "prisma": "7.8.0",
    "tsx": "4.23.1",
    "typescript": "6.0.3",
    "vitest": "4.1.10"
  }
}
